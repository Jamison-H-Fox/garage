import React from "react";
import styled from "styled-components";

const StyledSection = styled.section`
`

function Logout(props) { // eslint-disable-line

    return (
        <StyledSection>
            <h2>Logout Component</h2>
        </StyledSection>
    )
}

export default Logout;