import React from "react";
import styled from "styled-components";

const StyledSection = styled.section`
`

function Welcome(props) { // eslint-disable-line

    return (
        <StyledSection>
            <h2>Welcome Component</h2>
        </StyledSection>
    )
}

export default Welcome;