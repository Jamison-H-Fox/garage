import React from "react";
import styled from "styled-components";

const StyledSection = styled.section`
`

function Add(props) { // eslint-disable-line

    return (
        <StyledSection>
            <h2>Add Component</h2>
        </StyledSection>
    )
}

export default Add;