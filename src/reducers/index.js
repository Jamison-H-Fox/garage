import {  } from '../actions';

const initialState = {
    message: '',
}

function reducer(state = initialState, action) {
    switch(action.type) {
        default:
            return state;
    }
}

export default reducer;